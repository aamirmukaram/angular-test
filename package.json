{
  "name": "angular-test",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "postinstall": "npx json-server --watch db.json --middlewares ./validate.js",
    "test": "echo \"Error: no test specified\" && exit 1",
    "serve": "npm install && npx json-server --watch db.json --middlewares ./validate.js"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "json-server": "^0.14.0"
  }
}
